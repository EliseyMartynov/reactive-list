import React from "react";
import styles from "../styles/Header.module.css";

const Header = () => {
  return (
    <>
      <h1 className={styles.header}>Reactive list</h1>
    </>
  );
};

export default Header;
